<?xml version="1.0" encoding="utf-8"?>

<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@+id/add_tag_item"
          android:title="@string/add_tag_title"
          android:icon="@drawable/ic_new"
          android:showAsAction="ifRoom" />
    <!-- <item android:id="@+id/remove_tag_item"
          android:title="@string/remove_tag_title"
          android:showAsAction="never" /> -->

    <item android:id="@+id/refresh_item"
          android:title="@string/refresh_title"
          android:icon="@drawable/ic_refresh"
          android:showAsAction="ifRoom" />

    <item android:id="@+id/prefs_item"
          android:title="@string/prefs_item_title"
          android:showAsAction="never" />
</menu>